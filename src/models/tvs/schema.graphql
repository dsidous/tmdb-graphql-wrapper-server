extend type Query {
  tvs(query: String): [Tv!]
}

type Tv {
  id: ID!
  title: String!
  poster_path: String
  overview: String
  release_date: String
  genre_ids: [Int]
  genre_names: Genre_names
  backdrop_path: String 
  popularity: Float
  vote_count: Int
  vote_average: Float
}
